{
  "requiredEnv": ["CIRCLECI_TOKEN", "AWS_ACCOUNT_ID", "AWS_ROLE_ARN", "CIRCLE_OIDC_TOKEN"],
  "mcpServers": {
    "circleci-mcp-server": {
      "command": "npx",
      "args": ["-y", "@circleci/mcp-server-circleci@latest"],
      "env": {
        "CIRCLECI_TOKEN": "${CIRCLECI_TOKEN}",
        "CIRCLECI_BASE_URL": "https://circleci.com"
      }
    },
    "awslabs.aws-api-mcp-server": {
      "command": "bash",
      "args": [
        "-c",
        "pip install -q uv 2>/dev/null || true; OIDC_FILE=$(mktemp /tmp/oidc-XXXXXX.token); printf '%s' \"$CIRCLE_OIDC_TOKEN\" > \"$OIDC_FILE\"; export AWS_WEB_IDENTITY_TOKEN_FILE=\"$OIDC_FILE\"; uvx awslabs.aws-api-mcp-server@latest; rm -f \"$OIDC_FILE\""
      ],
      "env": {
        "CIRCLE_OIDC_TOKEN": "${CIRCLE_OIDC_TOKEN}",
        "AWS_ROLE_ARN": "${AWS_ROLE_ARN}",
        "AWS_REGION": "ap-northeast-1",
        "AWS_ACCOUNT_ID": "${AWS_ACCOUNT_ID}",
        "READ_OPERATIONS_ONLY": "true"
      }
    }
  }
}
